package test2

// AccountData — класс для объекта пользователя (логин/пароль)
type AccountData struct {
	Username string
	Password string
}

// TodoData — класс для объекта задачи
type TodoData struct {
	Title    string
	Deadline string
}
